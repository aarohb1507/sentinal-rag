# GCP Production Environment Variables
# Replace with your actual GCP service URLs after deployment

# Database (Cloud SQL)
DATABASE_URL=postgresql://sentinal_user:YOUR_PASSWORD@/sentinal_db?host=/cloudsql/YOUR_PROJECT_ID:asia-south1:sentinal-postgres

# Redis (Memorystore)
REDIS_HOST=10.0.0.3
REDIS_PORT=6379
REDIS_URL=redis://10.0.0.3:6379

# API Service (Cloud Run URL)
API_URL=https://sentinal-api-XXXXX-uc.a.run.app

# Worker Service (Cloud Run URL)
WORKER_URL=https://sentinal-worker-XXXXX-uc.a.run.app

# OpenAI API Key (required for embeddings and LLM)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Environment
NODE_ENV=production

# CORS Origins (add your frontend URL)
CORS_ORIGINS=https://sentinal-web-XXXXX-uc.a.run.app

# JWT Secret (generate a secure random string)
JWT_SECRET=your-secure-jwt-secret-here

# Log Level
LOG_LEVEL=info

# Rate Limiting
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000
